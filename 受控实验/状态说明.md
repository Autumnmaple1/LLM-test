# 受控实验（LLM 幻觉检测与缓解）— 交付状态

> 更新：2026-08-13（flash 主模型版本实验进行中）

## 实验设定（正式口径）
- **主模型 A = deepseek-v4-flash**（便宜、部署主力）；**交叉验证 B = deepseek-v4-pro**（仅交叉验证/重答时用）；judge = flash（thinking=False）
- 3 数据集：MMLU-Pro 100（分层）、TruthfulQA MC1 100、HLE 50 纯文本（mc12+em38）；固定种子 42
- 实验组：Bare / Self-Consistency K=3 / CoVe（自检对照）/ 本人方案（多角度重答+冲突风险+选择性修正）/ 跨模型消融（低温/高温/A+B）；RARR 未实现（无检索）
- 无外部证据注入；正确率为主指标；统计：按题 bootstrap、Wilson CI、配对检验
- 对照（裸答/全量/随机×30/选择性/Oracle）从 own 缓存构造（所有题均真实执行过修正）

## 数据完整度
| 数据集 | 状态 | 说明 |
|---|---|---|
| MMLU-Pro | ✅ 完整（7 组） | flash 主版已出汇总 |
| TruthfulQA | ✅ 完整（7 组） | flash 主版已出汇总 |
| HLE | ⏸️ 已停止（bare 42/50 留档） | em 难题长思考+空响应，成本/时间过高，仅保留部分数据作参考 |

## 已同步的原始数据（results/raw/，共 17 文件）
- mmlupro: 7 组（bare 100、sc 98、cove 99、own 93+6超时、low 99、high 96、cross 75+3超时+22空响应失败）
- truthfulqa: 7 组全部 100 行
- hle: bare 42/50（已停止）

## 已有汇总（本目录）
- `results/summary/truthfulqa_summary.md` — flash 主版 TruthfulQA 汇总
- `results/summary_proA_backup/` — **pro 主版本（已废弃，仅作对照存档）**：mmlupro/truthfulqa 完整 + hle 部分

## TruthfulQA（flash 主）核心数字
| 方法 | 正确率 | 净提升 | 误改率 |
|---|---|---|---|
| Bare | 84.0% | — | — |
| SC K=3 | 84.0% | +1.0pt | 0.0% |
| CoVe | 44.0% | **−35.0pt** | 55.7% |
| own | 84.0% | +1.0pt | 1.2% |
- 检测 AUROC 0.687 [0.554, 0.824]；30% 触发错误率 31.0% vs 全体 16.2%
- 跨模型消融最强：0.691 > 低温 0.659 > 高温 0.622
- 选择性 vs 随机 +0.5pt（p=1.00，不显著）；费用 0.001 元/题（pro 主 0.006）

## 复现命令（本地）
```bash
cd /mnt/d/python/llm_benchmark/controlled_exp
python3 src/sample.py
python3 src/run.py --dataset mmlupro --groups all --workers 16
python3 src/run.py --dataset truthfulqa --groups all --workers 16
python3 src/run.py --dataset hle --groups bare,own,low,high,cross --workers 16
python3 src/stats.py --dataset all
```

## 待办
- [ ] MMLU-Pro flash 版跑完 → 出汇总
- [ ] HLE flash 版跑完 → 出汇总
- [ ] 更新 overview.md 正式版（flash 主结论 + pro 主对照）
- [ ] 同步最终版到本目录
